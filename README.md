# Nora Fan — GitHub Pages portfolio

This folder contains a complete five-page personal platform. It needs no build step and can be published directly with GitHub Pages.

Pages included:

- `index.html` — platform homepage
- `academic.html` — research, education and academic CV
- `professional.html` — projects and capabilities
- `life.html` — personal notes, reading, culture and places
- `about.html` — profile, working principles and contact

## Publish

1. Upload all HTML files, `style.css`, and `script.js` to the root of your `username.github.io` repository.
2. In GitHub, open **Settings → Pages** and choose **Deploy from a branch**, `main`, `/ (root)`.
3. Wait a few minutes, then visit `https://username.github.io`.

## Replace before publishing

Search all HTML files for `TODO`. These locations need your real information:

- Add your email, LinkedIn URL, and GitHub URL in `about.html`.
- Put your CV at `assets/Nora-Fan-CV.pdf`, then remove `aria-disabled="true"`, the `is-disabled` class, and the “available soon” note.

You can also revise any project description to reflect your exact role, methods and outcomes once those details are ready.
